apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - route.yaml

patches:
  - path: scc-patch.yaml

images:
  - name: ghcr.io/eximeebpms/eximeebpms-bpm-platform
    newTag: run-1.2.0
