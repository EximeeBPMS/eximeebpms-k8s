apiVersion: v1
kind: Service
metadata:
  name: eximeebpms
  labels:
    app.kubernetes.io/name: eximeebpms
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: eximeebpms
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
